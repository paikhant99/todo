import 'package:flutter/material.dart';

class NotesPage extends StatelessWidget {
  const NotesPage({super.key});

  /*
    Comments - 
    FIXME : Change Later
  */
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Feature Ongoing'),
    );
  }
}
